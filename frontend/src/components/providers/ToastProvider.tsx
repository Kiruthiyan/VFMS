"use client";

// Placeholder provider — extend this for global state providers as needed
// Currently sonner Toaster (in layout.tsx) handles all toast notifications

export function ToastProvider() {
  return null;
}
